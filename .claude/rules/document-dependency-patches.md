---
paths:
  - "meta/patches/**"
---

# Document Dependency Patches

Adding or changing a patch in `meta/patches/` means updating `meta/patches/README.md` in the same commit, naming the upstream bug the patch fixes and the test that covers it.
